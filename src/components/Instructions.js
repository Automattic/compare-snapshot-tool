export default function Instructions() {
    return (
        <div className='instructions'>
            <h2>Upload a CSV to compare</h2>
            <p>This tool can display a side-by-side snapshot or preview of two URLs</p>
        </div>
      )
}